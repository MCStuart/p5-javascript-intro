type 

>> $ `browser-sync start --server -f -w` 

into terminal from inside './empty-example' to serve files with browserSync to live-reload upon file changes

** Mouse values and function calls **

* MOUSE VALUES
    mouseX
    mouseY
* MOUSE ACTION
    mouseIsPressed 
* MOUSE FUNCTIONS
    mousePressed()
    mouseMoved() 	
    mouseDragged()
    mouseReleased()
    mouseClicked() 	
    mouseScrolled() 	
* TOUCH VALUES
    touchX
    touchY
    touches[]
* TOUCH FUNCTIONS
    touchStarted()
    touchMoved()

`#p5_loading` is a reserved id for preload messages

** Browser functions and native JS **
    windowWidth / windowHeight
    displayWidth / displayHeight
    winMouseX / winMouseY
    fullscreen()

